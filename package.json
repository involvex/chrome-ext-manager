{
  "name": "ext-cli",
  "version": "1.0.0",
  "type": "module",
  "module": "index.ts",
  "bin": {
    "ext-cli": "./ext-cli.exe"
  },
  "scripts": {
    "build": "bun build src/cli.ts --compile --outfile ext-cli.exe",
    "dev": "bun run src/cli.ts",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^26.1.0"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  },
  "dependencies": {
    "commander": "^15.0.0",
    "node-forge": "^1.4.0"
  }
}
